[project]
name = "locked"
version = "0.2.0"
description = "Study toolkit application with Task Management and Study Workspace"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "starlette>=0.37.0",
    "uvicorn>=0.29.0",
    "jinja2>=3.1.3",
    "python-multipart>=0.0.9",
]

[project.optional-dependencies]
test = [
    "pytest>=8.0.0",
    "httpx>=0.27.0",
]
